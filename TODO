check recursive entity references produce expected result

switch off wxml warnings

remove all print statements

update list of exceptions in documentation

tidy up documentation

document no DOMTimeStamp

setFoXChecks - and how do we carry that around?
We check in m4 if the exception code starts with FoX, if it does we add a line saying something like:
if (np%ownerDocument%docExtras%FoX_checks) then
...

should FoX_checks be in implementation?

check liveNodelists on/off

forbid entityreferences in standalone docs
