Expand charset/make unicode string classes
Check unicode IO
Fix makeilfe to make m_common_format w/o optimization (if g95 is not fixed)
Document str properlyt
Intel zero string in m_common_format
pickup -DBROKEN_COMPILER automatically

Format all files coherently - done
