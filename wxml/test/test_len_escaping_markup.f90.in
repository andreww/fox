program test_len_escaping_markup

  use xmlf90_wxml, only : len_escaping_markup

  character(len=*), parameter :: c = &
TESTINPUT
  integer :: i

  i = len_escaping_markup(c)
  write(*,'(i0)') i

end program test_len_escaping_markup
